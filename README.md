# Telegram Dialog Analytics - Система аналитики диалогов

Расширенная система мониторинга и анализа диалогов Telegram с использованием AI.

## Основные возможности

### 1. Мониторинг диалогов
- Отслеживание неотвеченных сообщений клиентов
- Автоматическое сканирование каждые 3 минуты
- Фильтрация по статусам (новые, в работе, решено)
- Назначение ответственных менеджеров
- Добавление заметок к диалогам

### 2. Детальная аналитика
- **Анализ времени ответа**: 
  - Время ожидания первого ответа менеджера
  - SLA мониторинг (>1.5 часа = превышение)
  - Качество обслуживания (отлично/хорошо/приемлемо/долго/очень долго)
  
- **Анализ поведения менеджера**:
  - Проверка представления по имени
  - Наличие приветствия
  - Использование эмоджи
  - Маркеры вежливости (спасибо, пожалуйста, извините)

- **AI анализ с Google Gemini**:
  - Оценка профессионализма (1-10)
  - Анализ тона ответа
  - Качество приветствия
  - Рекомендации по улучшению

### 3. Визуализация данных
- Графики распределения времени ответа
- Диаграммы качества обслуживания
- Статистика за день
- Процент превышения SLA
- Процент представившихся менеджеров

### 4. Система уведомлений
- WebSocket real-time обновления
- Звуковые и визуальные алерты
- Модальные окна для критичных сообщений

## Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd telegram-analytics
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
```bash
cp .env.example .env
```

Отредактируйте файл `.env`:
```
API_ID=your_api_id
API_HASH=your_api_hash
GOOGLE_AI_KEY=AIzaSyBoYgQMK5cydwDThxovjsRdyUEgcIIu2_g
PORT=5000
```

### 4. Получение API credentials

#### Telegram API:
1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `API_ID` и `API_HASH`

#### Google AI API (опционально):
- API ключ уже предоставлен в .env.example
- Для получения своего ключа: https://makersuite.google.com/app/apikey

### 5. Первый запуск
```bash
python web_app.py
```

Откройте браузер: http://localhost:5000

## Создание сессии Telegram

1. При первом запуске откройте `/setup`
2. Введите номер телефона в формате +7XXXXXXXXXX
3. Введите код подтверждения из Telegram
4. Скопируйте `SESSION_STRING`
5. Добавьте в переменные окружения (Railway/Heroku) или в `.env`

## Структура проекта

```
.
├── web_app.py                    # Основное приложение Flask
├── sentiment_analyzer.py         # Базовый анализатор настроений
├── detailed_analyzer.py          # Детальный анализ времени ответа
├── google_ai_analyzer.py         # Google AI интеграция
├── manager.html                  # Страница управления диалогами
├── analytics.html                # Страница аналитики
├── messages.html                 # Страница уведомлений
├── setup.html                    # Страница создания сессии
├── requirements.txt              # Python зависимости
└── dialog_statuses.json         # Хранилище статусов (автосоздается)
```

## Использование

### Навигация

Система имеет 3 основные вкладки:

1. **Диалоги** (`/manager`) - управление неотвеченными диалогами
2. **Аналитика** (`/analytics`) - детальная статистика и анализ
3. **Уведомления** (`/messages`) - система алертов

### Работа с диалогами

1. **Новые диалоги** отображаются красным
2. **Критичные** (негатив или >2ч без ответа) имеют пульсацию
3. Для каждого диалога доступно:
   - Назначение ответственного
   - Добавление заметки
   - Открытие чата в Telegram
   - Создание задачи
   - Отметка как "Решено"

### Аналитика

На странице аналитики вы увидите:

- **Общая статистика**:
  - Всего диалогов за день
  - Среднее время ответа
  - Количество превышений SLA
  - % представившихся менеджеров

- **Графики**:
  - Распределение по времени ответа
  - Качество обслуживания

- **Детальный анализ каждого диалога**:
  - Время ожидания ответа
  - Представился ли менеджер
  - Наличие приветствия
  - Настроение клиента
  - Оценка профессионализма
  - AI рекомендации

## Критерии оценки

### Время ответа
- **Отлично**: < 15 минут
- **Хорошо**: 15-30 минут
- **Приемлемо**: 30-60 минут
- **Долго**: 1-1.5 часа
- **Очень долго**: > 1.5 часа ⚠️ SLA нарушен

### Профессионализм (AI оценка)
- **8-10**: Отличный уровень
- **6-7**: Хороший уровень
- **< 6**: Требуется улучшение

## API Endpoints

### GET /api/dialogs
Получить список неотвеченных диалогов с аналитикой

### GET /api/analytics
Получить полную аналитику за день

### POST /api/update_status
Обновить статус диалога
```json
{
  "dialog_id": 123456,
  "status": "task",
  "manager": "Влад",
  "note": "Клиент ждет звонка"
}
```

### GET /api/export_csv
Экспорт диалогов в CSV

## WebSocket события

### Клиент → Сервер
- `connect` - подключение
- `confirm_message` - подтверждение просмотра

### Сервер → Клиент
- `dialogs_update` - обновление списка диалогов
- `analytics_update` - обновление аналитики
- `new_unanswered` - новый неотвеченный диалог
- `status_changed` - изменение статуса

## Deployment

### Railway
1. Создайте новый проект
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения в Settings → Variables
4. Deploy автоматически запустится

### Heroku
```bash
heroku create your-app-name
heroku config:set API_ID=your_id
heroku config:set API_HASH=your_hash
heroku config:set SESSION_STRING=your_string
heroku config:set GOOGLE_AI_KEY=your_key
git push heroku main
```

## Автоматизация

Система автоматически:
- Сканирует диалоги каждые 3 минуты
- Очищает статусы отвеченных диалогов
- Отправляет real-time обновления всем подключенным клиентам
- Сохраняет статусы в JSON файл

## Настройка менеджеров

Отредактируйте в `web_app.py`:
```python
MANAGERS = ['Влад', 'Егор', 'Юлия']
```

Добавьте имена в `sentiment_analyzer.py`:
```python
MANAGER_NAMES = {
    'владислав': ['владислав', 'влад', 'vladislav'],
    'егор': ['егор', 'egor'],
    'юлия': ['юлия', 'юля', 'julia']
}
```

## Исключение чатов

Чтобы исключить определенные чаты из мониторинга, отредактируйте в `web_app.py`:
```python
EXCLUDED_KEYWORDS = [
    "бизнес в польше", 
    "законы", 
    "название_чата"
]
```

## Лицензия

MIT

## Поддержка

По вопросам и предложениям создавайте Issues в репозитории.
