# Быстрый старт

## 1. Установка

```bash
pip install -r requirements.txt
```

## 2. Настройка .env файла

Создайте файл `.env` на основе `.env.example`:

```
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash
GOOGLE_AI_KEY=AIzaSyBoYgQMK5cydwDThxovjsRdyUEgcIIu2_g
PORT=5000
```

Получить Telegram API credentials: https://my.telegram.org

## 3. Запуск

```bash
python web_app.py
```

Откройте браузер: http://localhost:5000

## 4. Первичная настройка

1. Откройте `/setup`
2. Введите номер телефона
3. Введите код из Telegram
4. Скопируйте SESSION_STRING
5. Добавьте SESSION_STRING в файл `.env`
6. Перезапустите приложение

## Возможности

### 📊 Страница "Аналитика" (/analytics)

**Главные метрики:**
- Всего неотвеченных диалогов за день
- Среднее время ответа на первое сообщение
- Количество превышений SLA (>1.5 часа)
- Процент менеджеров, которые представились

**Графики:**
- Распределение по времени ответа
- Качество обслуживания

**Для каждого диалога:**
- ⏱️ Время ответа (точное в минутах/часах)
- ✅/❌ Представился ли менеджер по имени
- 👋 Использовал ли приветствие
- 😊 Настроение клиента
- 🤖 Оценка профессионализма (1-10) от Google AI
- 💡 Рекомендации от AI

### Критерии SLA

**Время ответа:**
- < 15 мин = Отлично ✅
- 15-30 мин = Хорошо 👍
- 30-60 мин = Приемлемо 😐
- 1-1.5ч = Долго ⚠️
- > 1.5ч = Очень долго ❌ **НАРУШЕНИЕ SLA**

### Google AI анализ

Для каждого диалога Google AI автоматически оценивает:
- Профессионализм менеджера (1-10)
- Тон ответа (дружелюбный/нейтральный/формальный/холодный)
- Качество приветствия
- Дает конкретные рекомендации по улучшению

## Структура вкладок

1. **Диалоги** - управление неотвеченными сообщениями
2. **Аналитика** - детальная статистика и AI анализ ⭐
3. **Уведомления** - система алертов

## Что отслеживается

✅ Представился ли менеджер по имени  
✅ Сколько времени ждал клиент первого ответа  
✅ Превышение SLA (более 1.5 часов)  
✅ Настроение клиента  
✅ Качество обслуживания  
✅ Профессионализм менеджера  
✅ Рекомендации по улучшению от AI  

## Настройки

### Изменить порог SLA

В файле `detailed_analyzer.py` измените условие:
```python
is_overtime = delay_hours > 1.5  # Изменить 1.5 на нужное значение
```

### Добавить менеджеров

В файле `web_app.py`:
```python
MANAGERS = ['Влад', 'Егор', 'Ваше_Имя']
```

В файле `detailed_analyzer.py`:
```python
manager_names = {
    'владислав': ['владислав', 'влад'],
    'ваше_имя': ['ваше_имя', 'короткое_имя']
}
```

## Поддержка

Все работает автоматически:
- Сканирование каждые 3 минуты
- Real-time обновления
- Автоматическая очистка старых данных
- AI анализ для каждого диалога
